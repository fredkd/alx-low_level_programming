#include "main.h"
#include <stdio.h>

/**
 * print_rev - prints string in reverse
 * @*s : parameter used
 * return: void
 */

void print_rev(char *s)
{
	int i;
	
	for (i = 0; s[i] = '\0'; i--)
	{
		putchar(*s);
	}
	putchar('\n');
}

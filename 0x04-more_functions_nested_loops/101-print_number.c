#include "main.h"

/**
 * print_number - prints number
 * @n: param used
 * Return: void
 */

void print_number(int n)
{
	if (n > 0)
	{
		_putchar(n);
	}
	else if (n = 0)
	{
		_putchar(n);
	}
	else
		_putchar(n);
	_putchar('\n')
}

#include "main.h"

/**
 * _islower - function that checks for lowercase characters
 * @c" single letter input
 * Return: Always success
 */

int _islower(int c)
{
	
 	(c >= 'a' && c <= 'z')
	{
		return(1);
	} else 
		return(0);
	

}
 
 

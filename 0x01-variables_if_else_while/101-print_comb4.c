#include <stdio.h>

/**
 * main - this is the start of the code
 * Return: 0 always true
 */

int main(void)
{
	int i;

	for (i = 100; i <= 999; i++)
	{
		putchar(i);
	}
	putchar(',');

	return (0);
}

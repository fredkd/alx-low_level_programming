#include <stdio.h>

/**
 * main - this is the start of the code
 * @i: char used
 * @J: char used
 * Return: 0 Always (success)
 */
int main(void)
{
	char i;
	char j;

	for (i = 'a'; i <= 'z'; i++)
	{
		putchar(i);
	}
	for (j = 'A'; J <= 'Z'; j++)
	{
		putchar(j);
	}
	return (0);
}

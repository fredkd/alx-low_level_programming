#include <stdio.h>

/**
 * main - This is the start of the code
 * Return: 0 always success
 */
int main(void)
{
	char c;

	for (i = 'a'; i <= 'z'; ++i)

		putchar(i);
	for (i = 'A'; i <= 'Z'; ++i)

		putchar(i);

	putchar('\n')


	return(0);
}
